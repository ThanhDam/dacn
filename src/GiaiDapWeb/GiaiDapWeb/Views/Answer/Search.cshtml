@{
    ViewBag.Title = "Tìm kiếm";
    Layout = null;
}

@*@if (ViewData.Model.Count == 0)
{
    <h3 style="color:red;">Xin lỗi!!!!</h3>
    <h4> Không tìm thấy</h4>
    <h3> @Html.Action("Index", "Answer")</h3>
}
    
    @{
        if (Request.Cookies["myCookies"] != null)
        {
            HttpCookie ck = new HttpCookie("myCookies");
            Session["maTaiKhoan"] = ck["name"].ToString();
        }
    }
    @if (Session["maTaiKhoan"] != null)
    {

        if (Session["maTaiKhoan"].ToString() == "admin")
        {
            <script type="text/javascript">
            $(function () {
                $('.RemoveLink').click(function () {
                    var x = $(this).attr("data-id");
                    if (confirm("Đối tượng sẽ bị xóa.Bạn có muốn không ?")) {
                        $.ajax({
                            type: "POST",
                            url: '@Url.Action("Delete", "Product")',
                            data: { id: x }
                        });
                        $("#row-" + x).remove();
                    }
                })
            })
            </script>
            <div style="margin-left:20px;">
                @Html.ActionLink("Thêm Mới", "Create")
            </div>
        }
    }
    <table class="table table-striped" style="float:right">
    <thead>
        <tr>
            <th></th>
            <th>#</th>
            <th>Mã câu hỏi</th>
            <th>Mã số giáo vụ</th>
            <th>Nội dung</th>
            <th>Thời gian</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in ViewData.Model)
        {
            <tr>
                <td width="20%"></td>
                <td width="10%">@Html.DisplayFor(modelItem => item.id)</td>

                <td width="15%">@Html.DisplayFor(modelItem => item.question_id)</td>
                <td width="10%">@Html.DisplayFor(modelItem => item.ministry_id)</td>
                <td width="20%">@Html.DisplayFor(modelItem => item.text)</td>
                <td width="5%">@Html.DisplayFor(item => item.time_respond)</td>
                <td width="20%">
                    @Html.ActionLink("Sửa", "Edit", new { id = item.id }) |
                    @Html.ActionLink("Xem", "Details", new { id = item.id }) |
                    @Html.ActionLink("Xóa", "Delete", new { id = item.id })
                </td>
            </tr>


        }
    </tbody>
</table>*@



    @*<div style="clear:both"></div>*@
    @*<div id="searchBox">
            <%using (Html.BeginForm("Tim", "Home"))
            {%>
            <input type=text name=search />
            <%} %>
        </div>*@


